1 # General Guidelines

- Don't include any superfluous PHP Annotations, except ones that start with `@` for typing variable
- SUPER IMPORTANT: At the very end, **always**, but **always** run `composer test`